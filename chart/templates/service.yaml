apiVersion: v1
kind: Service
metadata:
  name: {{ include "warframe-wishlist.fullname" . }}
  labels:
    {{- include "warframe-wishlist.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "warframe-wishlist.selectorLabels" . | nindent 4 }}
